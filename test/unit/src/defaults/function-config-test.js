let { test } = require('node:test')
let functionConfig = require('../../../../src/defaults/function-config')

test('Set up env', t => {
  t.plan(1)
  t.assert.ok(functionConfig, 'Function config module is present')
})

test('Function config returns correct defaults', t => {
  t.plan(13)
  let result = functionConfig()
  t.assert.equal(Object.keys(result).length, 12, 'Got correct number of properties')
  t.assert.ok(result.timeout, 'Got timeout')
  t.assert.ok(result.memory, 'Got memory')
  t.assert.ok(result.runtime, 'Got runtime')
  t.assert.ok(result.handler, 'Got handler')
  t.assert.ok(result.state, 'Got state')
  t.assert.ok(result.concurrency, 'Got concurrency')
  t.assert.ok(result.storage, 'Got storage')
  t.assert.ok(result.layers, 'Got layers')
  t.assert.ok(result.policies, 'Got policies')
  t.assert.ok(result.shared, 'Got shared')
  t.assert.ok(result.env, 'Got env')
  t.assert.ok(result.architecture, 'Got architecture')
})
